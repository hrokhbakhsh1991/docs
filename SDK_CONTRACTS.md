# SDK Contracts

The canonical **SDK contract reference** for workspace authors lives in the workspace-sdk package:

**[`packages/workspace-sdk/SDK_CONTRACTS.md`](./packages/workspace-sdk/SDK_CONTRACTS.md)**

It documents all exported interfaces, types, manifest hook functions, expected I/O, and poorly typed exports for `@app-tour/workspace-sdk`.
