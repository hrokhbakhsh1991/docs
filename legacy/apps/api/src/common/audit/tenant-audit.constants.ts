/** Max rows per `GET …/workspaces/:tenantId/audit-events` page (UI). */
export const TENANT_AUDIT_LIST_MAX_LIMIT = 200;

/** Default page size for tenant audit list UI. */
export const TENANT_AUDIT_LIST_DEFAULT_LIMIT = 50;
