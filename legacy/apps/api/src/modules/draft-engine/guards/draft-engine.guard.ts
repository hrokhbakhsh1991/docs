/**
 * MAP-facing guard name. Enforces {@link DraftEngineAccessPolicy} (Draftable scope via JWT + policy).
 */
export { DraftEngineAbilitiesGuard as DraftEngineGuard } from "./draft-engine-abilities.guard";
