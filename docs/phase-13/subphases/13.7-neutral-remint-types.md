# 13.7 — Neutral wizard photo remint types (closure)

> **DEC:** DEC-P13-009  
> **Status:** DONE — 2026-06-18  
> **Parent:** [Phase 13 README](../README.md)

## Problem

`TourCloneHydrationResult.photoRemintPlan` uses `DenaliPhotoRemintPlanEntry` in SDK — product-named type on a workspace-neutral contract.

## Solution

```typescript
export type WizardPhotoRemintPlanEntry = {
  readonly sourceStorageKey: string;
  readonly destStorageKey: string;
  readonly oldPhotoId: string;
  readonly newPhotoId: string;
  readonly contentType?: string;
};

/** @deprecated Use WizardPhotoRemintPlanEntry */
export type DenaliPhotoRemintPlanEntry = WizardPhotoRemintPlanEntry;
```

`TourCloneHydrationResult.photoRemintPlan` → `readonly WizardPhotoRemintPlanEntry[]`.

Denali `remint-denali-clone-photos.ts` imports SDK type; `DenaliPhotoRemintPlanEntry` remains a deprecated alias for backward compat.

Web `tour-clone-hydrate-logic.ts` uses `WizardPhotoRemintPlanEntry`.

## Acceptance (13.7)

- [x] SDK exports `WizardPhotoRemintPlanEntry`
- [x] `TourCloneHydrationResult` uses neutral type
- [x] Denali re-exports alias only (no duplicate struct)
- [x] Web hydrate logic uses neutral type
- [x] Builds + targeted tests green

## Phase 13 closure

Subphases 13.0–13.7 complete wizard plugin-neutral closure for Denali reference path. Urban proof (create-flow with media) remains future work.
