# 13.8 — Phase 13 remediation closure

> **DEC:** DEC-P13-010  
> **Status:** DONE — 2026-06-18  
> **Parent:** [Phase 13 README](../README.md)

## Problem

Subphases 13.0–13.7 landed implementation but **machine DoD** (roadmap §12) failed on boundary, SDK allowlist, ingress harness, and test drift.

## Solution

### B — Web boundary

`wizard-draft-envelope-hooks.ts` is plugin-neutral (no product imports). Denali callers inject fallbacks. `denali-draft-normalize-remote.ts` uses `resolveBootstrapWorkspacePlugin`. Shadow tombstone uses dynamic import.

### C — SDK allowlist

Tombstone exports added to `ALLOWED_ROOT_RUNTIME_EXPORTS` in `contract.spec.ts`.

### D — Ingress harness

`pluginPayloadForStorageIngress()` JSON round-trip strips runtime-only functions (`draftTombstone`, `wizardHost`) before `parseWorkspacePluginFromStorage`.

### E — UNIFY-08

Create client wires `normalizeRemoteEnvelope` callback (calls `normalizeWizardRemoteEnvelope`); flat-edit uses `normalizeDenaliRemoteEnvelope` shim.

### F — P13-7

`tour-clone-hydrator.contract.spec.ts` — P13-7-01/02.

## Acceptance (13.8)

- [x] `workspace-boundary.spec.ts` green
- [x] SDK `contract.spec.ts` P0-GATE-04 green
- [x] Ingress harness tests green (SDK + platform-core)
- [x] `denali-draft-unification-closure` WEB-P11-UNIFY-08 green
- [x] P13-7-01/02 spec green
- [x] Phase 13 regression pack green (57 tests)
- [x] Machine grep gates (roadmap §12) green

### Out-of-scope `test:changed` failures (pre-existing, not Phase 13)

| Test | Area |
|------|------|
| `cold-start latency` | API perf / flaky |
| `Phase 3.2 integrity audit` | API storage audit |
| `phase-8.contract` | platform-core baseline drift |
| `WEB-DENALI-WIZARD-08` | Tailwind in composite UI |
| `WEB-11.8-02` | Denali rules parity |
| `WEB-P11-3-15` | Draft DELETE proxy |
| `WEB-9.6-WIZ-03` | Wizard template gate |

## Non-goals (13.8)

- Clone remint API dispatch — future phase
- `mergeDraftEnvelope` SDK hook
