# SUBPHASE 4.5 — Platform events (in-process bus)

> **P4-E:** P4-E-EVT-01 · **CI:** [`../ci.md`](../ci.md)

```yaml
subphase: "4.5"
dag_node: P4-5
prerequisites: ["4.2"]
parallelizable: true
parallel_with: ["4.4"]
p4_e_ids: [P4-E-EVT-01]
ci_commands:
  - pnpm --filter @app-tour/platform-events run test
observability_ref: ../appendices/observability.md
pr_label: "Phase: 4.5"
execution_index: ../audits/execution-action-index.md
precision_pack: ../appendices/PRECISION-DOC-INDEX.md
ready_spec: ../audits/SUBPHASE-READY-SPEC.md
machine_readable: true
completion_proof:
  type: p4_e
  schema: ../appendices/subphase-completion-schema.md
  enforcement: [P4-E-EVT-01]
  prove_with:
    - pnpm --filter @app-tour/platform-events run test
    - "integration POST /tours emits TourCreated with tenantId"
package: "@app-tour/platform-events"

api_required:
  publishDomainEvent:
    params: "{ tenantId, type, payload }"
    returns: void
  subscribe:
    params: "type, handler"
    returns: void

rules:
  - "FORBIDDEN cross-tenant dispatch — MUST throw"
  - "handler idempotent eventId dedupe minimal 4"

hook:
  service: CanonicalTourService
  trigger: "after successful persist"
  event_type: TourCreated

steps:
  - step: "4.5-S1"
    action: "CREATE packages/platform-events with publish subscribe"
  - step: "4.5-S2"
    action: "HOOK CanonicalTourService post-persist TourCreated"
  - step: "4.5-S3"
    action: "UNIT test subscriber receives correct tenantId P4-E-EVT-01"
  - step: "4.5-S4"
    action: "INTEGRATION test publish after POST /tours postgres path"

exit_criteria_4_5:
  - "unit platform-events spec PASS"
  - "integration POST /tours emits TourCreated with tenantId"
```
