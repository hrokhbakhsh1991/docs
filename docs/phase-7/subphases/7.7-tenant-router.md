# SUBPHASE 7.7 — TenantConnectionRouter (Silo)

> **REQ:** [`../audits/verification-matrix.md`](../audits/verification-matrix.md) · **DEC:** DEC-P7-004, DEC-P7-011 · **Spec:** [`../appendices/TENANT-ROUTER-SPEC.md`](../appendices/TENANT-ROUTER-SPEC.md)

```yaml
subphase: "7.7"
dag_node: P7-7
prerequisites: ["7.5", "7.6"]
parallelizable: false
blocks_until_complete: ["7.8"]
enforcement_req_ids: ["REQ-P7-021", "REQ-P7-022", "REQ-P7-023", "REQ-P7-032"]
forbidden_ids: ["P7-F-004"]
action_ids: ["P7-7-A01", "P7-7-A02", "P7-7-A03"]
enforcement_rule_ids: ["RULE-P7-004"]
repo_status: SPEC_ONLY
completion_proof:
  type: router_tests
  prove_with:
    - infra/sql/003_tenant_routes.sql applied
    - packages/tenant-kernel/test/tenant-connection-router.spec.ts
  prerequisite_status: "7.5 + 7.6 VERIFIED_BEHAVIORAL (TG-P7-005)"
```

## Exit criteria

- `tenant_routes` DDL migrated (`infra/sql/003_tenant_routes.sql`)
- `TenantRoute` extended per TENANT-ROUTER-SPEC (schemaName, useRls)
- `TenantConnectionRouter.resolveRoute(tenantId)` pool + silo cases
- Misconfigured silo row fails fast

## Forbidden

- Ad-hoc DATABASE_URL in apps/api per request
- Silo as default for all tenants (P7-F-004)

---

## Actions

```yaml
P7-7-A01:
  description: Add infra/sql/003_tenant_routes.sql migration
  validation:
    - table exists after migrate
    - CHECK tier IN ('pool','silo')

P7-7-A02:
  description: Implement TenantConnectionRouter in tenant-kernel
  validation:
    - extends route.ts TenantRoute interface
    - maps database_url → databaseUrl

P7-7-A03:
  description: tenant-connection-router.spec.ts pool + silo + error cases
  validation:
    - pool uses env DATABASE_URL + useRls true
    - silo uses override URL
    - null silo config throws
```

---

## Primary spec: tenant router

| Case              | Proves                                 | REQ        |
| ----------------- | -------------------------------------- | ---------- |
| Migration applied | `\d tenant_routes` in psql             | REQ-P7-021 |
| Pool tenant       | unit test default route                | REQ-P7-022 |
| Silo tenant       | unit test override URL                 | REQ-P7-023 |
| Spec alignment    | TenantRoute matches TENANT-ROUTER-SPEC | REQ-P7-032 |

```bash
pnpm --filter @app-tour/tenant-kernel exec node --import tsx --test test/tenant-connection-router.spec.ts
```

See [`../appendices/TENANT-ROUTER-SPEC.md`](../appendices/TENANT-ROUTER-SPEC.md).
