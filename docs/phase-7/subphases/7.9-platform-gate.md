# SUBPHASE 7.9 — Platform DoD Gate

> **REQ:** [`../audits/verification-matrix.md`](../audits/verification-matrix.md) · **MAP:** §12 · **Rubric:** [`../appendices/FORENSIC-RUBRIC.md`](../appendices/FORENSIC-RUBRIC.md)

```yaml
subphase: "7.9"
dag_node: P7-9
prerequisites: ["7.8"]
parallelizable: false
enforcement_req_ids: ["REQ-P7-027", "REQ-P7-028", "REQ-P7-029", "REQ-P7-030", "REQ-P7-035"]
forbidden_ids: ["P7-F-005"]
action_ids: ["P7-9-A01", "P7-9-A02", "P7-9-A03", "P7-9-A04", "P7-9-A05"]
enforcement_rule_ids: ["RULE-P7-010"]
repo_status: IN_PROGRESS
completion_proof:
  type: phase_gate
  prove_with:
    - .github/workflows/phase-7-gate.yml green (7.8 adversarial + ci:integrity)
    - pnpm run phase-7:gate
    - docs/audits/phase-7-zero-debt-forensic-audit.mdoc verdict PASS
    - reports/phase-7-gate-*.json
  prerequisite_status: "merge_7_9_requires all VERIFIED_BEHAVIORAL (TG-P7-007)"
```

## Exit criteria

- `pnpm run phase-7:gate` exit 0 (build + test + `phase-6:guard` + `phase-7:guard`; denested — does not nest `phase-6:gate`)
- Forensic audit score ≥ 8 per FORENSIC-RUBRIC
- Platform DoD MAP §11 row 7 checklist complete
- IMPLEMENTATION-TRUTH updated — urban VERIFIED_BEHAVIORAL

## Forbidden

- Doc-guard-only closure (P7-F-005)
- Forensic PASS while urban package absent

---

## Actions

```yaml
P7-9-A01:
  description: Run phase-7:gate full chain
  validation:
    - exit 0
    - reports/phase-7-gate-*.json ok true

P7-9-A02:
  description: Complete forensic audit — score ≥ 8
  validation:
    - phase-7-zero-debt-forensic-audit.mdoc verdict PASS
    - FORENSIC-RUBRIC weighted sum ≥ 8

P7-9-A03:
  description: Update IMPLEMENTATION-TRUTH all hot_paths VERIFIED
  validation:
    - urban package VERIFIED_BEHAVIORAL
    - router VERIFIED_BEHAVIORAL

P7-9-A04:
  description: ci:integrity + Platform DoD MAP checklist
  validation:
    - ci:integrity exit 0
    - MIGRATION-MAP §11 row 7 boxes checked

P7-9-A05:
  description: Optional total-paranoid-audit if MAP requires
  validation:
    - reports/total-paranoid-audit-YYYY-MM-DD.md when mandated
```

---

## Primary spec: platform gate

| Check         | Command                    | Pass         | REQ        |
| ------------- | -------------------------- | ------------ | ---------- |
| Phase 7 gate  | `pnpm run phase-7:gate`    | exit 0       | REQ-P7-027 |
| Contract spec | `phase-7.contract.spec.ts` | exit 0       | REQ-P7-028 |
| Forensic      | FORENSIC-RUBRIC score      | ≥ 8          | REQ-P7-029 |
| ci:integrity  | `pnpm run ci:integrity`    | exit 0       | REQ-P7-030 |
| Hot paths     | IMPLEMENTATION-MAP audit   | all VERIFIED | REQ-P7-035 |

```bash
# Local full chain (slow):
pnpm run phase-7:gate
pnpm run ci:integrity

# GitHub Actions (recommended — workflow phase-7-gate job platform-dod):
#   after ci-integrity + adversarial-p0 green → pnpm run phase-7:platform-gate
pnpm run phase-7:platform-gate
```

## GitHub Actions (7.9)

Workflow [`.github/workflows/phase-7-gate.yml`](../../../.github/workflows/phase-7-gate.yml) job **`platform-dod`** runs after `ci-integrity` and `adversarial-p0` succeed. It executes `phase-7:platform-gate` (phase-6:guard + phase-7:guard) — build/test chain already satisfied by `ci:integrity` in the same run. Artifacts: `reports/phase-7-platform-gate-*.json`, `reports/phase-7-gate-*.json`.

`workflow_dispatch` input `run_platform_dod` (default `true`) skips the job when set `false`.
