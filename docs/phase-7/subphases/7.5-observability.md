# SUBPHASE 7.5 — Observability + Runbook

> **REQ:** [`../audits/verification-matrix.md`](../audits/verification-matrix.md) · **DEC:** DEC-P7-005 · **Runbook:** [`../appendices/OBSERVABILITY-RUNBOOK.md`](../appendices/OBSERVABILITY-RUNBOOK.md)

```yaml
subphase: "7.5"
dag_node: P7-5
prerequisites: ["7.4"]
parallelizable: true
parallel_with: ["7.6"]
blocks_until_complete: ["7.7"]
enforcement_req_ids: ["REQ-P7-015", "REQ-P7-016", "REQ-P7-017", "REQ-P7-033"]
forbidden_ids: ["P7-F-005"]
action_ids: ["P7-5-A01", "P7-5-A02", "P7-5-A03"]
enforcement_rule_ids: ["RULE-P7-005"]
repo_status: SPEC_ONLY
completion_proof:
  type: runbook_audit
  prove_with:
    - scripts/guards/audit-log-fields.mjs --phase 7
    - appendices/OBSERVABILITY-RUNBOOK.md
```

## Exit criteria

- MAP §10.2 fields in structured logs: `requestId`, `tenantId`, `workspaceType`, `tenantTier`, `durationMs`
- OBSERVABILITY-RUNBOOK alert matrix populated with numeric thresholds
- Optional OTel documented in env-runtime-matrix (not blocking — DEC-P7-012)
- **No** urban-only log branches in apps/api

---

## Actions

```yaml
P7-5-A01:
  description: Extend apps/api logging middleware with workspaceType + tenantTier
  validation:
    - scripts/guards/audit-log-fields.mjs --phase 7 exit 0

P7-5-A02:
  description: Complete OBSERVABILITY-RUNBOOK — P0/P1 alerts with thresholds
  validation:
    - all §10.2 fields documented
    - on-call steps present

P7-5-A03:
  description: Optional OTel traceparent propagation (DEC-P7-012)
  validation:
    - OTEL_EXPORTER_OTLP_ENDPOINT in env-runtime-matrix
    - skipped in production if unset — not blocking 7.9
```

---

## Primary spec: observability

| Check            | Command                                                      | Pass         | REQ        |
| ---------------- | ------------------------------------------------------------ | ------------ | ---------- |
| Log field audit  | `node scripts/guards/audit-log-fields.mjs --phase 7`         | exit 0       | REQ-P7-015 |
| Runbook complete | review OBSERVABILITY-RUNBOOK.md                              | all sections | REQ-P7-016 |
| Generic layer    | `rg "workspaceType === 'urban'" apps/api/src/common/logging` | no matches   | REQ-P7-017 |
| OTel optional    | env-runtime-matrix OTEL\_\* documented                       | present      | REQ-P7-033 |

```bash
node scripts/guards/audit-log-fields.mjs --phase 7
```
