# SUBPHASE 7.0 — Phase 7 Entry Gate

> **SOURCE OF TRUTH (actions):** this file · **REQ:** [`../audits/verification-matrix.md`](../audits/verification-matrix.md) · **Bridge:** [`../appendices/phase-6-bridge.md`](../appendices/phase-6-bridge.md)

```yaml
subphase: "7.0"
dag_node: P7-0
prerequisites: [phase-6:gate]
parallelizable: false
blocks_until_complete: ["7.1"]
enforcement_req_ids: ["REQ-P7-001", "REQ-P7-002", "REQ-P7-003"]
forbidden_ids: ["P7-F-002"]
action_ids: ["P7-0-A01", "P7-0-A02", "P7-0-A03", "P7-0-A04"]
enforcement_rule_ids: ["RULE-P7-007"]
ci_commands:
  - pnpm run phase-6:gate
  - pnpm run guard:import-boundary
entry_yaml: ../../../reports/phase-7-entry-verified.yaml
repo_status: VERIFIED_ENTRY
completion_proof:
  type: gate
  prove_with:
    - pnpm run phase-6:gate
    - reports/phase-7-entry-verified.yaml
  note: "doc_pack_score PASS is independent of phase_6_gate — both required for 7.0 DoD"
```

## Exit criteria

- `pnpm run phase-6:gate` exit 0 recorded in yaml
- Denali bootstrap pattern documented (6.5) — urban depends on it
- No `URBAN_*` in `apps/api` generic layer
- No runtime `import` from `legacy/` in trunk `apps/*`

## Forbidden

- Start 7.1 while 7.0 yaml `phase_6_gate: PENDING`
- Claim 7.0 PASS if `phase-6:gate` red

---

## Actions

```yaml
P7-0-A01:
  description: Run phase-6:gate and capture exit code + report path
  validation:
    - exit_code == 0
  outputs:
    - phase_7_entry.phase_6_gate.status: PASS

P7-0-A02:
  description: Update reports/phase-7-entry-verified.yaml all required fields
  inputs:
    - IMPLEMENTATION-TRUTH Phase 6 rows
  validation:
    - verified_at set when PASS
    - critical_spec_quality: 96 documented

P7-0-A03:
  description: Verify no urban creep in apps/api core
  validation:
    - pnpm run guard:import-boundary exit 0
    - rg 'URBAN_' apps/api/src --glob '!**/workspace/**' → no matches
  outputs:
    - phase_7_entry.no_urban_core_creep: PASS

P7-0-A04:
  description: Assert zero legacy/ runtime imports in trunk apps
  validation:
    - rg "from ['\"]legacy/" apps/api apps/web → no matches
```

---

## Primary spec: entry yaml + guards

| Check            | Command                                    | Pass                        | REQ        |
| ---------------- | ------------------------------------------ | --------------------------- | ---------- |
| Phase 6 gate     | `pnpm run phase-6:gate`                    | exit 0                      | REQ-P7-001 |
| Entry ledger     | `reports/phase-7-entry-verified.yaml`      | `phase_6_gate.status: PASS` | REQ-P7-002 |
| No urban creep   | `pnpm run guard:import-boundary`           | exit 0                      | REQ-P7-003 |
| No legacy import | `rg "from ['\"]legacy/" apps/api apps/web` | no matches                  | REQ-P7-003 |

```bash
pnpm run phase-6:gate
pnpm run guard:import-boundary
```
