# SUBPHASE 7.2 — Genericity Proof (no platform-core diff)

> **REQ:** [`../audits/verification-matrix.md`](../audits/verification-matrix.md) · **DEC:** DEC-P7-001, DEC-P7-009

```yaml
subphase: "7.2"
dag_node: P7-2
prerequisites: ["7.1"]
parallelizable: false
blocks_until_complete: ["7.3"]
enforcement_req_ids: ["REQ-P7-006", "REQ-P7-007", "REQ-P7-008"]
forbidden_ids: ["P7-F-002"]
action_ids: ["P7-2-A01", "P7-2-A02", "P7-2-A03"]
enforcement_rule_ids: ["RULE-P7-001", "RULE-P7-008"]
repo_status: SPEC_ONLY
completion_proof:
  type: diff_guard
  prove_with:
    - packages/workspaces/urban/test/phase-7.contract.spec.ts
    - reports/phase-7-genericity-baseline.yaml
    - git diff ${baseline_sha} -- packages/platform-core
```

## Exit criteria

- Baseline SHA recorded in `reports/phase-7-genericity-baseline.yaml`
- `phase-7.contract.spec.ts` asserts zero urban-only platform-core changes
- `git diff ${baseline_sha} -- packages/platform-core` empty at merge

## Forbidden

- 7.3 bootstrap before 7.2 VERIFIED_BEHAVIORAL (TG-P7-003)
- Any `if (workspaceType === 'urban')` in platform-core

---

## Actions

```yaml
P7-2-A01:
  description: Record platform-core baseline SHA before urban merge (P7-X-A01)
  outputs:
    - reports/phase-7-genericity-baseline.yaml with baseline_sha
  validation:
    - baseline_sha is valid git ref

P7-2-A02:
  description: Add packages/workspaces/urban/test/phase-7.contract.spec.ts
  validation:
    - asserts no URBAN branches in platform-core source
    - fails if platform-core diff non-empty vs baseline

P7-2-A03:
  description: CI guard — rg URBAN in platform-core
  validation:
    - rg 'URBAN|urban_event' packages/platform-core --glob '!*.md' → no product branches
    - RULE-P7-001 review PASS
```

---

## Primary spec: platform-core diff guard

| Check         | Command                                                                                               | Pass       | REQ        |
| ------------- | ----------------------------------------------------------------------------------------------------- | ---------- | ---------- |
| Contract spec | `pnpm --filter @app-tour/workspace-urban exec node --import tsx --test test/phase-7.contract.spec.ts` | exit 0     | REQ-P7-006 |
| Git diff      | `git diff $(yq .baseline_sha reports/phase-7-genericity-baseline.yaml) -- packages/platform-core`     | empty      | REQ-P7-007 |
| Static guard  | `rg 'URBAN' packages/platform-core`                                                                   | no matches | REQ-P7-008 |

```bash
git diff main -- packages/platform-core
pnpm --filter @app-tour/workspace-urban exec node --import tsx --test test/phase-7.contract.spec.ts
```
