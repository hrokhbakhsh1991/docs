# SUBPHASE 6.0 — Phase 6 Entry Gate

> **SOURCE OF TRUTH (actions):** this file · **REQ:** [`../audits/verification-matrix.md`](../audits/verification-matrix.md) · **Bridge:** [`../appendices/phase-5-bridge.md`](../appendices/phase-5-bridge.md)

```yaml
subphase: "6.0"
dag_node: P6-0
prerequisites: [phase-5:gate]
parallelizable: false
blocks_until_complete: ["6.1"]
enforcement_req_ids: ["REQ-P6-001", "REQ-P6-002", "REQ-P6-003", "REQ-P6-020", "REQ-P6-025"]
forbidden_ids: ["P6-F-002"]
action_ids: ["P6-0-A01", "P6-0-A02", "P6-0-A03", "P6-0-A04", "P6-0-A05"]
ci_commands:
  - pnpm run phase-5:gate
  - pnpm run guard:import-boundary
entry_yaml: ../../../reports/phase-6-entry-verified.yaml
repo_status: PARTIAL
completion_proof:
  type: gate
  prove_with:
    - pnpm run phase-5:gate
    - reports/phase-6-entry-verified.yaml
  note: "doc_pack_score PASS is independent of phase_5_gate — both required for 6.0 DoD"
```

## Exit criteria

- `pnpm run phase-5:gate` exit 0 recorded in yaml
- `phase_5_behavioral_minimum` documents truth from [`IMPLEMENTATION-TRUTH.md`](../audits/IMPLEMENTATION-TRUTH.md) (5.2 minimum VERIFIED)
- No `DENALI_*` in `apps/api` generic layer (depcruise / guards)
- `starter` plugin resolution still green
- No runtime `import` from `legacy/` in trunk `apps/*`

## Forbidden

- Start 6.1 while 6.0 yaml `PENDING`
- Claim 6.0 PASS if `phase-5:gate` red
- Hide open 5.3–5.5 without `phase_5_behavioral_minimum` note in yaml

---

## Actions

```yaml
P6-0-A01:
  description: Run phase-5:gate and capture exit code + report path
  validation:
    - exit_code == 0
  outputs:
    - phase_6_entry.phase_5_gate.status: PASS

P6-0-A02:
  description: Update reports/phase-6-entry-verified.yaml all required fields
  inputs:
    - IMPLEMENTATION-TRUTH Phase 5 rows
  validation:
    - verified_at set when PASS
    - phase_5_behavioral_minimum reflects 5.2 VERIFIED + 5.3-5.5 state

P6-0-A03:
  description: Verify no Denali creep in apps/api core
  validation:
    - depcruise / phase-5 FORBIDDEN-008..011 still pass
  outputs:
    - phase_6_entry.no_denali_core_creep: PASS

P6-0-A04:
  description: Assert zero legacy/ runtime imports in trunk apps
  validation:
    - grep + import-boundary guard

P6-0-A05:
  description: Confirm starter resolve tests still pass
  validation:
    - apps/api resolve-workspace-plugin.spec.ts exit 0
```

---

## Primary spec: entry yaml + guards

| Check            | Command                                    | Pass                        | REQ        |
| ---------------- | ------------------------------------------ | --------------------------- | ---------- |
| Phase 5 gate     | `pnpm run phase-5:gate`                    | exit 0                      | REQ-P6-001 |
| Entry ledger     | `reports/phase-6-entry-verified.yaml`      | `phase_5_gate.status: PASS` | REQ-P6-002 |
| No Denali creep  | `pnpm run guard:import-boundary`           | exit 0                      | REQ-P6-003 |
| No legacy import | `rg "from ['\"]legacy/" apps/api apps/web` | no matches                  | REQ-P6-020 |

```bash
pnpm run phase-5:gate
pnpm run guard:import-boundary
# edit reports/phase-6-entry-verified.yaml — set verified_at when all PASS
```

## Research / industry notes

Entry gate aligns with **Strangler Fig week 1–2**: facade exists (resolver), observability on legacy vs new paths starts at 6.5 — see [`../../research/phase-6-denali-workspace-research.md`](../../research/phase-6-denali-workspace-research.md) §3.2.
