# SUBPHASE 6.5 — API/Web bootstrap

> **DEC-P6-003** · **REQ:** REQ-P6-013, REQ-P6-014, REQ-P6-024, REQ-P6-026

```yaml
subphase: "6.5"
dag_node: P6-5
prerequisites: ["6.2", "6.3", "6.4"]
blocks_until_complete: ["6.6", "6.7", "6.8"]
enforcement_req_ids: ["REQ-P6-013", "REQ-P6-014", "REQ-P6-024", "REQ-P6-026"]
action_ids: ["P6-5-A01", "P6-5-A02", "P6-5-A03", "P6-5-A04"]
repo_status: VERIFIED_BEHAVIORAL
pr_label: "Phase: 6.5"
dod_ref: phase-6-enforcement.md subphase_dod "6.5"
enforcement_rule_ids: ["RULE-P6-005", "RULE-P6-006", "RULE-P6-013"]
completion_proof:
  prove_with:
    - pnpm --filter @apps/api exec node --import tsx --test test/denali-workspace-plugin.spec.ts
    - pnpm --filter @app-tour/workspace-sdk test test/denali-workspace-binding.contract.spec.ts
  prerequisite_status: "6.2 + 6.3 + 6.4 VERIFIED_BEHAVIORAL (TG-P6-005)"
```

## Target wiring

| App | Today                                                 | Target                                         |
| --- | ----------------------------------------------------- | ---------------------------------------------- |
| API | `resolveWorkspacePluginForType("denali")` → NOT_BOUND | `getDenaliWorkspacePlugin()`                   |
| Web | starter-only registry                                 | dynamic `import('@app-tour/workspace-denali')` |

### Implementation layout (6.5)

| Path                                                   | Role                                                                                                                  |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| `packages/workspace-sdk/.../workspace-type-binding.ts` | `DEFAULT_WORKSPACE_TYPE_BINDINGS` adds `{ workspaceType: "denali", pluginId: "denali" }`                              |
| `apps/api/src/workspace/workspace-plugins.ts`          | Eager register starter + denali                                                                                       |
| `apps/api/src/workspace/shadow-validate-denali.ts`     | `SHADOW_VALIDATE_DENALI` guard — forbidden in production                                                              |
| `apps/web/src/bootstrap/lazy-denali-plugin.ts`         | **Only** file allowed to reference `@app-tour/workspace-denali` (dynamic import)                                      |
| `apps/web/next.config.ts`                              | `transpilePackages` includes `@app-tour/workspace-denali`                                                             |
| `dependency-cruiser.config.js`                         | `apps-api-allowed-packages` + `apps-web-*` allow `workspaces/denali` (guard:architecture parity)                      |
| `apps/api/src/tours/canonical-validation-sync.ts`      | Map `RuleContext.dimensions` from `plugin.ruleSet.matrixDimensions` (starter=`variant`, denali=`category`+`duration`) |

**Strangler step:** deploy resolver with flag off → internal tenants → ramp (research §3.2).

## Exit criteria

- Integration: create tour with `workspace_type=denali` persists + validates
- Web wizard loads Denali plugin for denali tenant host
- `resolveWorkspacePluginIdForType("denali") === "denali"` (workspace-sdk contract)
- **No** fallback to starter when tenant is denali

## Forbidden

- `WorkspaceStrategyRegistry` / `denali_pilot` branches in api core
- Shadow validate (`REQ-P6-024`) enabled in production

---

## Actions

```yaml
P6-5-A01:
  description: Register denali in apps/api resolve-workspace-plugin.ts
  validation:
    - apps/api/test/denali-workspace-plugin.spec.ts

P6-5-A02:
  description: Extend apps/web workspace-plugin-registry lazy load
  validation:
    - bundle/chunk contains denali only when needed

P6-5-A03:
  description: Optional SHADOW_VALIDATE_DENALI env — dual validate log diff (non-prod)
  validation:
    - disabled when NODE_ENV=production

P6-5-A04:
  description: Update workspace-sdk denali-workspace-binding.contract.spec.ts expectations
  validation:
    - REQ-P6-026 pass
```

---

## Primary spec: `denali-workspace-plugin.spec.ts`

| Case                                                     | Proves                      | REQ        |
| -------------------------------------------------------- | --------------------------- | ---------- |
| `resolveWorkspacePluginForType("denali")` returns plugin | not NOT_BOUND               | REQ-P6-013 |
| create tour with `workspace_type=denali` validates       | engine uses denali registry | REQ-P6-007 |
| starter tenant still uses starter plugin                 | no regression               | REQ-P6-025 |
| `SHADOW_VALIDATE_DENALI` off in production config        | safety                      | REQ-P6-024 |

```bash
pnpm --filter @apps/api exec node --import tsx --test test/denali-workspace-plugin.spec.ts
```

**Forbidden:** `getStarterWorkspacePlugin()` when resolved type is `denali`.
