# SUBPHASE 6.1 — Denali package shell

> **DEC-P6-002** · **REQ:** REQ-P6-004, REQ-P6-005, REQ-P6-027

```yaml
subphase: "6.1"
dag_node: P6-1
prerequisites: ["6.0"]
blocks_until_complete: ["6.2"]
enforcement_req_ids: ["REQ-P6-004", "REQ-P6-005", "REQ-P6-027"]
forbidden_ids: ["P6-F-003"]
action_ids: ["P6-1-A01", "P6-1-A02", "P6-1-A03", "P6-1-A04"]
ci_commands:
  - pnpm --filter @app-tour/workspace-denali build
  - pnpm --filter @app-tour/workspace-denali test
repo_status: SPEC_ONLY
pr_label: "Phase: 6.1"
reference_workspace: packages/workspaces/starter
dod_ref: phase-6-enforcement.md subphase_dod "6.1"
completion_proof:
  prove_with:
    - pnpm --filter @app-tour/workspace-denali build
    - pnpm --filter @app-tour/workspace-denali test test/phase-6.contract.spec.ts
  prerequisite_status: "6.0 phase_5_gate PASS"
```

## Target layout

```text
packages/workspaces/denali/
  package.json              # name: @app-tour/workspace-denali
  src/index.ts              # export getDenaliWorkspacePlugin
  src/denali.plugin.ts      # WorkspacePlugin
  src/denali.plugin.manifest.json  # optional — contract guard (research §4.2)
  theme/tokens.css
  test/phase-6.contract.spec.ts
```

**Replace:** `DENALI_BREACH_PROBE` — probe remains only until this subphase merges.

## Exit criteria

- `pnpm --filter @app-tour/workspace-denali build` exit 0
- `getDenaliWorkspacePlugin()` implements `WorkspacePlugin` (mirror starter surface)
- `phase-6.contract.spec.ts` — **VERIFIED_SCAFFOLD** minimum (export surface)
- README states **product workspace** (not probe)

## Forbidden

- Denali-only changes in `platform-core` for shell only
- Runtime import from `legacy/`

---

## Actions

```yaml
P6-1-A01:
  description: Scaffold package.json, tsconfig, src/denali.plugin.ts from starter pattern
  outputs:
    - getDenaliWorkspacePlugin export
  validation:
    - build passes

P6-1-A02:
  description: Add phase-6.contract.spec.ts asserting plugin id, theme path, registry hook placeholders
  validation:
    - test exit 0 (scaffold allowed)

P6-1-A03:
  description: Update packages/workspaces/denali/README.md — remove probe-only claim
  validation:
    - phase-6-guard p6_denali_probe_honesty still passes post-replace (wording: product workspace)

P6-1-A04:
  description: Wire package into pnpm workspace + root build graph
  validation:
    - pnpm build includes workspace-denali
```

---

## Primary spec: `phase-6.contract.spec.ts` (scaffold → behavioral)

| Case                                   | Proves                    | REQ             |
| -------------------------------------- | ------------------------- | --------------- |
| exports `getDenaliWorkspacePlugin`     | REQ-P6-004                | function typeof |
| plugin.id === `denali`                 | registry id               | REQ-P6-005      |
| theme path resolvable                  | `theme/tokens.css` exists | REQ-P6-010 prep |
| no `DENALI_BREACH_PROBE` in public API | probe removed             | REQ-P6-027      |

```bash
pnpm --filter @app-tour/workspace-denali build
pnpm --filter @app-tour/workspace-denali test
```

**Status enum after scaffold green:** `VERIFIED_SCAFFOLD` — not 6.9 closure.
