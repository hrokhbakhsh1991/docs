# SUBPHASE 6.2 — Registry and rules port

> **SOURCE OF TRUTH:** this file · **Checklist:** [`../appendices/LEGACY-PORT-CHECKLIST.md`](../appendices/LEGACY-PORT-CHECKLIST.md) · **REQ:** REQ-P6-006–009,021,023,015

```yaml
subphase: "6.2"
dag_node: P6-2
prerequisites: ["6.1"]
blocks_until_complete: ["6.3", "6.4", "6.5"]
parallelizable: false
enforcement_req_ids:
  ["REQ-P6-006", "REQ-P6-007", "REQ-P6-008", "REQ-P6-009", "REQ-P6-021", "REQ-P6-015"]
enforcement_rule_ids: ["RULE-P6-004", "RULE-P6-015"]
action_ids: ["P6-2-A01", "P6-2-A02", "P6-2-A03", "P6-2-A04", "P6-2-A05", "P6-2-A06"]
ci_commands:
  - pnpm --filter @app-tour/workspace-denali run denali:codegen
  - pnpm --filter @app-tour/workspace-denali test test/registry-parity.spec.ts
repo_status: SPEC_ONLY
pr_label: "Phase: 6.2"
dod_ref: phase-6-enforcement.md subphase_dod "6.2"
completion_proof:
  type: req
  prove_with:
    - pnpm --filter @app-tour/workspace-denali test test/registry-parity.spec.ts
    - pnpm --filter @app-tour/workspace-denali run denali:codegen && git diff --exit-code packages/workspaces/denali/src/rules/generated
  prerequisite_status: "6.1 VERIFIED_SCAFFOLD"
```

## Exit criteria

- LEGACY-PORT-CHECKLIST P0 rows ported
- `registry-parity.spec.ts` — ≥3 golden fixtures match legacy `evaluateFormRules` output
- `validateCanonical` FAIL closed on invalid Denali canonical
- `denali:codegen` — CI fails on dirty `generated/`
- **No** `@repo/denali-domain` import in trunk apps

---

## Primary spec: `registry-parity.spec.ts`

| Case               | Input                          | Proves                  | REQ        |
| ------------------ | ------------------------------ | ----------------------- | ---------- |
| Field registry ids | legacy count snapshot          | no missing ids          | REQ-P6-006 |
| evaluateFormRules  | `golden/tour-minimal.json`     | deepEqual legacy output | REQ-P6-015 |
| Invalid canonical  | mutated fixture                | throws validation error | REQ-P6-007 |
| ACL boundary       | `rg legacy` outside `src/acl/` | zero imports            | REQ-P6-008 |

```bash
pnpm --filter @app-tour/workspace-denali run denali:codegen
git diff --exit-code packages/workspaces/denali/src/rules/generated
pnpm --filter @app-tour/workspace-denali test test/registry-parity.spec.ts
```

---

## Actions

```yaml
P6-2-A01:
  description: Port P0 rows from LEGACY-PORT-CHECKLIST into plugin src/
  inputs:
    - legacy/packages/denali-domain/src/registry/denaliFieldRegistryData.ts
    - legacy/packages/denali-domain/src/registry/DenaliFieldRegistry.ts
  outputs:
    - packages/workspaces/denali/src/field-registry/
  validation:
    - field id manifest equals legacy snapshot

P6-2-A02:
  description: Create src/acl/ — normalizeLegacyTripDetails, toCanonicalDocument
  inputs:
    - legacy trip_details samples from audit fixtures
  outputs:
    - packages/workspaces/denali/src/acl/*.ts
  validation:
    - rg 'from.*legacy' packages/workspaces/denali --glob '!acl/**' → 0

P6-2-A03:
  description: Add test/fixtures/golden/*.json (3 files per SMOKE-SCENARIO-MAP)
  outputs:
    - tour-minimal.json, tour-template-overlay.json, tour-publish-ready.json
  validation:
    - REQ-P6-023 files committed

P6-2-A04:
  description: Wire fieldRegistry + rules into getDenaliWorkspacePlugin()
  inputs:
    - packages/platform-core PlatformWizardEngine
  outputs:
    - validateCanonical(plugin, doc) in plugin unit tests
  validation:
    - invalid fixture → CANONICAL_VALIDATION_FAILED

P6-2-A05:
  description: Add package.json script denali:codegen from denaliRegistryCodegen pattern
  outputs:
    - "denali:codegen" script
  validation:
    - REQ-P6-009 git diff clean after run

P6-2-A06:
  description: Cross-link phase-5-canonical-schema.md extension_phase_6 addendum (doc only)
  validation:
    - link exists in schema doc
```
