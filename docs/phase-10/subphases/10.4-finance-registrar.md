# Subphase 10.4 — Denali finance HTTP registrar + web loader cleanup

> **Phase:** 10.4 · **Status:** DONE (S1–S3)  
> **Goal:** R1 finance routes dispatch via `tryDispatchWorkspaceRoutes` — not inline `app.ts` blocks; web plugin load via codegen only.

## Completed (2026-06-08)

| Task | Change |
| ---- | ------ |
| P4-T01 | `denali-workspace-routes.ts` + registrar dispatch for `/finance/*` |
| P4-T01 | `AppDeps.financeService` optional; `resolveLazyFinanceService` in registrar |
| P4-T01 | `lazy-route-handlers.ts` loads `@app-tour/workspace-denali/http` |
| P4-T02 | Handlers + schemas in `packages/workspaces/denali/src/http/`; API shim + `configureDenaliFinanceHttpHost` |
| P4-T03 | Removed `lazy-denali-plugin.ts` / `lazy-urban-plugin.ts` — sole entry `workspace-plugin-loaders.generated.ts` |
| P4-T04 | `workspace-boundary.spec.ts` allowlist = generated loader only |
| P4-T05 | `workspace-tour-write-dispatch.ts` — publish-field gate by workspace type |
| P4-T06 | `mergeUrbanCanonicalPatchData` in `@app-tour/workspace-urban/tours` |
| P4-T07 | `test/finance-route-registrar.spec.ts` · `urban-tours-bypass-gate` · `workspace-tour-write-dispatch` |

## Routes (R1 — FINANCE-OPS-UX)

| Method | Path | Handler |
| ------ | ---- | ------- |
| GET | `/finance/reports/summary` | `handleFinanceSummary` |
| GET | `/finance/reports/open-payments` | `handleFinanceOpenPayments` |
| GET | `/finance/reports/ledger-events` | `handleFinanceLedgerEvents` |
| GET | `/finance/payments` | `handleFinanceListPayments` |
| POST | `/finance/payments/manual` | `handleFinanceCreateManualPayment` |
| POST | `/finance/receipts` | `handleFinanceSubmitReceipt` |
| PATCH | `/finance/receipts/:receiptId/review` | `handleFinanceReviewReceipt` |
| GET | `/finance/receipts/:receiptId/url` | `handleFinanceReceiptUrl` |

Handlers remain in `apps/api/src/denali-finance/` (Prisma adapters) until P4-T02 package move.

## Manifest

`packages/workspaces/denali/workspace.manifest.json` → `http.prefix` `/finance`

## Verification

```bash
rg '"/finance/' apps/api/src/app.ts   # → 0
pnpm --filter @apps/api exec node --import tsx --test test/finance-route-registrar.spec.ts
pnpm --filter @apps/web exec node --import tsx --test test/workspace-boundary.spec.ts
pnpm --filter @apps/api exec node --import tsx --test test/denali-finance-outbox.integration.spec.ts
```

## S2 — Package move (P4-T02)

Handlers + Zod request schemas live in `packages/workspaces/denali/src/http/`. API host wires Prisma adapters via `configureDenaliFinanceHttpHost()` (mirror `configureUrbanHttpHost`).

| Package | File | Role |
| ------- | ---- | ---- |
| denali | `http/finance.routes.ts` | R1 HTTP handlers |
| denali | `http/schemas/finance-request.schemas.ts` | Zod parsers |
| denali | `http/ports/finance-service.port.ts` | Service port (no Prisma) |
| denali | `http/host-ports.ts` / `host-runtime.ts` | Host injection |
| api | `http/configure-denali-finance-http-host.ts` | Host port adapter (mirror urban) |
| api | ~~`denali-finance/finance.routes.ts`~~ | **removed** — import `@app-tour/workspace-denali/http` |
| api | ~~`denali-finance/finance.schemas.ts`~~ | **removed** — schemas from workspace package |
| api | `denali-finance/finance.service.ts` | Prisma-backed service (host infra until DEC data) |

## S3 — Urban tour write hooks (P4-T05/T06)

Urban-specific tour PATCH logic lives in `@app-tour/workspace-urban/tours`. API host dispatches by `workspaceType` via `apps/api/src/tours/workspace-tour-write-dispatch.ts` — no `workspaceType === "urban"` in `canonical-tour.service.ts` or `tours.routes.ts`.

| Package | Export | Role |
| ------- | ------ | ---- |
| urban | `tours/tour-write-hooks.ts` | `urbanTourPatchTouchesPublishFields` + owner surface id |
| urban | `tours/canonical-patch-merge.ts` | `mergeUrbanCanonicalPatchData` |
| api | `tours/workspace-tour-write-dispatch.ts` | workspace-type → hook registry |

## S4 — Manifest-derived registrar rows (P4-T12)

Static `/finance/*` rows in `apps/api/src/http/denali-workspace-routes.ts` are built from `FINANCE_HTTP_ROUTE_MANIFEST` via `workspace-route-manifest-bridge.ts`. Parameterized receipt paths derive regex from manifest `:receiptId` segments — drift fails at module load.

Urban mirror: `urban-workspace-routes.ts` ← `URBAN_HTTP_ROUTE_MANIFEST`.

`guard-openapi-dispatch-parity` checks both workspace manifests cover `DISPATCH_ROUTES` inventory paths.

## Deferred (S5)

