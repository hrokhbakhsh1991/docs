# Subphase 10.7 — Enforcement + DX

> **Phase:** 10.7 · **Status:** DONE (scaffold) · Guest guard bundle: [guest-plugin-conformance](../../dev/guest-plugin-conformance.md)

## Tooling

| Command | Role |
| ------- | ---- |
| `pnpm run workspace:create -- <id>` | Scaffold starter workspace (`L0` manifest — plugin entry only) |
| `pnpm run workspace:create -- <id> --guest` | Scaffold **guest-ready** workspace (`L3` manifest + catalog HTTP stubs, registration flow compose surface, member profile, dev smoke tenant) |
| `pnpm run generate:workspace-registry` | Regenerate SDK/API/web loaders |
| `pnpm run generate:workspace-registry --check` | CI freshness (via `phase-10:guard`) |
| `pnpm run phase-10:guard` | Outbox/app.ts/registry invariants + certification (H) + theme import budget (I1) + plugin load cache (I2) — **11 checks** |
| `pnpm run guard:workspace-certification` | Phase H CERT-04 proof matrix + manifest tier sync |
| `pnpm run guard:guest-plugin-conformance` | PF-4 aggregator (registry fresh, fail-closed resolvers, generated banners, E2E hook manifest, conformance dual-verify) |
| `pnpm run phase-g-h:fast-track` | Phase G+H closure bundle before DEV→main PR ([workspace-certification.mdoc](../../dev/workspace-certification.mdoc)) |
| `pnpm run phase-i:closure` | G+H regression + I1/I2 guards — required before merge PR |
| `pnpm run phase-g-h:handoff` | Print merge PR checklist (no `gh` auth) |
| `pnpm run phase-g-h:create-pr` | DEV→main PR after `phase-i:closure` (needs `gh auth login`) |

## Guards (P7-T03..T05)

- `apps/api/scripts/guard-workspace-registry-imports.mjs` — `get*WorkspacePlugin` only in generated registry
- `scripts/guards/phase-10-guard.mjs` — outbox product-free · `app.ts` path-free · registry fresh · urban/denali HTTP shims removed · `http/configure-*-http-host.ts` present · `apps/api/src/workspace-finance/` host-infra allowlist (DEC-P10-006) · `guard-workspace-certification` (Phase H)

### DEC-P10-006 allowlist (workspace-finance host infra)

`p10_workspace_finance_host_infra` lists **top-level** names under `apps/api/src/workspace-finance/` that may exist. Product shims stay forbidden; host infra may grow when finance WS packages land (capability registry, obligation bindings, receipt upload, paid-without-ledger detection, wallet credit, recon/, WS5/WS6 onboarding specs, Case read provider façades under `case/` + `case-read/`, free-collection apply specs, cancel-pending-payment HTTP specs, commercial-quote host factory + memory store). When adding a new top-level file, update `WORKSPACE_FINANCE_HOST_INFRA_ALLOWLIST` in `scripts/guards/phase-10-guard.mjs` in the same PR and keep this paragraph honest.

**Allowlist additions (CI closure after finance Case land):** `case`, `case-read`, `apply-free-collection-after-booking-approve.spec.ts`, `finance-cancel-pending-payment-http.spec.ts`. Denali Case-read mapper imports must go through `workspace-finance-case-read-bindings.generated.ts` (and payment-collection through obligation bindings) so `guard-api-workspace-isolation` stays at allowlist size **0**.

**Allowlist additions (commercial quote host, CQ-1B / CQ-2B):** `create-commercial-quote-service.ts` and `finance-commercial-quote-store.ts`. These are **host composition**, not Denali/Urban product shims:

| File | Role | Why host, not workspace package |
| ---- | ---- | ------------------------------- |
| `create-commercial-quote-service.ts` | Factory `createCommercialQuoteServiceWithMemberDiscount` wires `CommercialQuoteService` (`@app-tour/finance-core`) to host adapters: registration freeze context + identity membership-discount read | Adapters live under `workspace-finance/infrastructure/`; factory is the API-side composition root for CQ-2B. Workspace plugins must not construct Prisma/memory quote services. |
| `finance-commercial-quote-store.ts` | Process-wide in-memory quote version map for `STORAGE_DRIVER=memory` and unit tests (CQ-1B / CQ-1C) | Durable path is `PrismaCommercialQuoteRepository`. Memory map must sit next to `finance-schedule-store.ts` / `in-memory-finance.repository.ts` so HTTP host + booking gates share one process store. |

Leaving them off the allowlist after User + Finance + Discount landed made `p10_workspace_finance_host_infra` FAIL (`phase-10-guard` red on `main`) even though the files are host infra. Product HTTP still enters through `http/configure-finance-http-host.ts` + `@app-tour/finance-http`.
- `pnpm run guard:guest-plugin-conformance` — PF-4 aggregator (registry fresh, fail-closed resolvers, generated banners, E2E hook manifest, conformance dual-verify)

## CI

[`.github/workflows/phase-10-guard.yml`](../../../.github/workflows/phase-10-guard.yml) on PR paths touching workspaces / generated registries / `scripts/codegen/workspace-registry/**` (Phase G modular entry).

## Drop-in smoke (P7-T06)

`test/fixtures/workspaces/climbing-club/workspace.manifest.json` + `scripts/test/workspace-registry-drop-in.spec.mjs` — validates fixture binding merge **without** `packages/workspaces/climbing-club/` on trunk. Trunk discovery expectations include scaffold workspaces `acme` and `harbor`; Phase I2 web-loader cache `WORKSPACE_PLUGIN_LOAD_CACHE_MAX_ENTRIES` equals the product-trunk plugin count (currently **8**).

## Guest workspace scaffold (`--guest`, PF-3)

`pnpm run workspace:create -- <id> --guest` writes a package that satisfies **guest conformance L3** after `pnpm run generate:workspace-registry`:

| Manifest block | Purpose |
| -------------- | ------- |
| `guestConformance.productionTier` | Explicit `stub` — production onboarding denied until promotion (Phase H) |
| `http` + `httpRoutes` | Catalog list/detail + registration POST paths (`GET /{id}/catalog`, `GET /{id}/catalog/:tourId`, `POST /{id}/registrations`) |
| `catalogPresentation` | Marketing list/detail gates (defaults: `cityFilter: false`, detail sections off) |
| `catalogRegistrationFlow` | Compose mode with `reuseAuthStepsFrom: "shared"` + local `intake`/`done` React steps |
| `memberProfile` | Portal `/me/profile` field visibility (minimal: editable `displayName`, read-only `email`) |
| `operatorCapabilities` | Explicit fail-closed operator feature declarations (`false`) until product implementation |
| `devBootstrap.pluginTenantIds` | Dev-only tenant UUID → plugin map (deterministic hash per workspace id) |
| `devBootstrap.smokeTenant` | Exports `${CONST}_SMOKE_TENANT_ID` / `${CONST}_SMOKE_SUBDOMAIN` from package root |
| `guestThemeStylesheets.marketing` | Placeholder marketing theme CSS |

Stub TypeScript/React files are included so `@app-tour/workspace-<id>` builds and codegen imports resolve (`/catalog-registration-flow`, `/http`, smoke exports). HTTP handlers return `501 WORKSPACE_GUEST_STUB` until product logic lands.

Verification after scaffold:

```bash
pnpm install
pnpm run generate:workspace-registry
pnpm run guard:workspace-registry-fresh
pnpm --filter @app-tour/workspace-<id> run build
node --test scripts/test/workspace-create-guest.spec.mjs
pnpm run guard:guest-plugin-conformance
pnpm run guard:guest-consumer-deps
```

When promoting a guest workspace to trunk (reference: `packages/workspaces/guest-club`), wire workspace package dependencies on consumers that import generated registries:

| Consumer | Dependency |
| -------- | ---------- |
| `packages/workspace-plugin-host` | `@app-tour/workspace-<id>` (registration flow + intake codegen imports) |
| `apps/api` | `@app-tour/workspace-<id>` + extend `prebuild` workspace build filter |
| `apps/web` | `@app-tour/workspace-<id>` (devDependency — dynamic plugin loader) |
| `apps/marketing` | `@app-tour/workspace-<id>` (guest theme CSS bootstrap) |
