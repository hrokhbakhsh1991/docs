# Subphase 10.7 — Enforcement + DX

> **Phase:** 10.7 · **Status:** DONE

## Tooling

| Command | Role |
| ------- | ---- |
| `pnpm run workspace:create -- <id>` | Scaffold `packages/workspaces/<id>/` |
| `pnpm run generate:workspace-registry` | Regenerate SDK/API/web loaders |
| `pnpm run generate:workspace-registry --check` | CI freshness (via `phase-10:guard`) |
| `pnpm run phase-10:guard` | Outbox/app.ts/registry invariants |

## Guards (P7-T03..T05)

- `apps/api/scripts/guard-workspace-registry-imports.mjs` — `get*WorkspacePlugin` only in generated registry
- `scripts/guards/phase-10-guard.mjs` — outbox product-free · `app.ts` path-free · registry fresh · urban/denali HTTP shims removed · `http/configure-*-http-host.ts` present · `denali-finance/` host-infra allowlist (DEC-P10-006)

## CI

[`.github/workflows/phase-10-guard.yml`](../../../.github/workflows/phase-10-guard.yml) on PR paths touching workspaces / generated registries.

## Drop-in smoke (P7-T06)

`test/fixtures/workspaces/climbing-club/workspace.manifest.json` + `scripts/test/workspace-registry-drop-in.spec.mjs` — validates fixture binding merge **without** `packages/workspaces/climbing-club/` on trunk.
