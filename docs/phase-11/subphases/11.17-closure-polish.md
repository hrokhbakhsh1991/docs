# Subphase 11.17 — Closure polish

> **Status:** DONE (2026-06-11) · **DEC-P11-016**

## Goal

Close remaining Phase 11 UX/data gaps after 11.16:

1. **Wizard draft audit** — settings page listing all workspace drafts + per-key event timeline (not only `/tours/new`).
2. **`compatibleCategories`** on tour theme API rows — Denali wizard catalog filters consume server-derived categories instead of web-only maps.

## Wizard drafts settings

| Layer | Path |
| ----- | ---- |
| Module | `wizard_drafts` in Denali settings manifest |
| UI | `GET /settings/wizard-drafts` |
| Data | Existing BFF `GET /api/workspaces/{id}/drafts` + `…/events` |

## Tour theme `compatibleCategories`

Derived from Denali `formProfile` → tour category allowlist (`theme-compatible-categories.ts`).  
Enriched on `GET /settings/resources/tour_themes` for Denali tenants only.

## Acceptance

| ID | Assertion |
| -- | --------- |
| WEB-P11-17-01 | Settings wizard-drafts lists index rows |
| WEB-P11-17-02 | Expanding row shows events timeline |
| API-P11-17-01 | Tour theme list includes `compatibleCategories` for Denali smoke tenant |
