# Subphase 11.8 — Denali rules parity hardening

**Charter:** [denali-rules-parity.md](../denali-rules-parity.md)  
**DEC:** DEC-P11-009

## Acceptance

- [x] WEB-DENALI-TR-03 — dong visible for `bus` + `allowPersonalCar`
- [x] `participants.nationalIdRequired` renders as standalone checkbox on non-mountain tours
- [x] Draft push clears ghost `dongAmount` / `peakHeight` after visibility toggle
- [x] `fieldRulesOverlay` changes rule matrix (not noop)
- [x] Destination select prefills `tripDetails.overview.peakHeight` when catalog has `altitudeM`
- [x] Gear list filtered by equipment `category` vs tour category
- [x] Theme list filtered by `formProfile` vs workspace profile
- [x] Submit builds Denali canonical payload (`prepareDenaliTourCreatePayload`)

## Verify

```bash
pnpm --filter @app-tour/workspace-denali exec node --import tsx --test test/template-overlay.spec.ts
cd apps/web && node --import tsx --test test/denali-transport-logic.spec.ts
```
