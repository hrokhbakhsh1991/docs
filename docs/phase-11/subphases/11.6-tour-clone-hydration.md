# Subphase 11.6 — Tour clone hydration

> **Authority:** [`tour-clone-hydration.md`](../tour-clone-hydration.md) · **DEC-P11-007**

## Scope

Port operator duplicate flow: `?clone=tourId` → fetch canonical tour → Denali wizard draft with `(Copy)` title and gear catalog filter.

## Tasks

| ID | Task | Status |
| -- | ---- | ------ |
| 11.6-T1 | `packages/workspaces/denali/src/clone/` — canonical → wizard draft transform | DONE |
| 11.6-T2 | `workspace-sdk` optional `TourCloneHydrator` on `WorkspacePlugin` | DONE |
| 11.6-T3 | `new-tour-wizard-client` — `useSearchParams().get("clone")` fetch + hydrate | DONE |
| 11.6-T4 | Loading UI `wizard.clone.loading` | DONE |
| 11.6-T5 | Gear stale filter + title `(Copy)` suffix | DONE |
| 11.6-T6 | `tour-clone-hydrate.spec.ts` + `WEB-9.3-04` hydrate assertion | DONE |

## Acceptance

- Duplicate link unchanged: `/tours/new?clone={id}`
- Denali clone hydrates `title` with ` (Copy)` from golden canonical fixture
- Gear rows outside active catalog are removed when ids are provided
- Non-Denali plugins ignore `?clone=` (template prefill + remote draft unchanged — `WEB-P11-6-06`)
- Clone replaces remote draft via `clearDraft()` before hydrate (`DRAFT_VERSION_CONFLICT` guard)

## Next

**11.8** — rules parity hardening.
