# 14.0b — Template gate hooks

## Charter

Move Denali template invariant imports from `wizard-template-gate-logic.ts` into `wizardHost.normalizeWizardTemplateGate`.

## SDK hook

`WorkspaceWizardHostHooks.normalizeWizardTemplateGate` — see `workspace-wizard-template-gate.ts`.

## Acceptance

- `rg '@app-tour/workspace-denali' apps/web/src/tours/wizard-template-gate-logic.ts` → empty
- Default `workspaceFormProfile` is `platform_default` when hook absent
- `buildDefaultPublishedWizardSteps` uses `plugin.wizard.steps[0].stepId` when present, else `resolveWizardTemplateGateDefaultPublishedStepId(pluginId)` from manifest codegen — **no** `pluginId === "denali"` branch in `wizard-template-gate-logic.ts`
