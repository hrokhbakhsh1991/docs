# 14.4 — Urban proof + TCK + closure

## Charter

Prove second workspace (Urban) loads through generic orchestrator; extension TCK validates `wizardHost` shapes; machine gate closes Phase 14.

## Urban manifest

```json
"wizardI18n": { "messageNamespace": "urban" }
```

Urban uses platform validation/review surfaces (`validationSurfaceId: "platform"`) — no custom composite/review codegen entries required.

## Acceptance

- `urban-wizard-create-smoke.spec.ts` green
- `wizard-host-tck.spec.ts` green for `denali`, `urban`, `starter`
- `bash scripts/phase-14-wizard-gate.sh` prints `PHASE_14_WIZARD_DOD_OK`
- `docs/phase-14/README.md` status DONE
