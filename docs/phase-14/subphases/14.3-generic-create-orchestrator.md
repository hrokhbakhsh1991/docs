# 14.3 — Generic create orchestrator

## Charter

`WorkspaceCreateTourWizardClient` — shared draft sync + host + submit for all workspaces.

## Router

`new-tour-wizard-client.tsx` uses manifest `wizardCreate.extendedChrome` instead of `pluginId === "denali"`.

## Acceptance

- `workspace-create-tour-shell.tsx` delegates to orchestrator; no Denali catalog import
- `create-tour.server.ts` passes payload through unchanged; `prepareSubmitPayload` runs client-side only
- Orchestrator uses `useWorkspaceDraft` + `DraftSyncChrome` + `navLocked`/`draftSyncStatus` on host (P14-3-T09)
- Shared page chrome extracted to `create-tour-wizard-chrome.tsx` for Denali reuse (P14-3-T05 partial)
