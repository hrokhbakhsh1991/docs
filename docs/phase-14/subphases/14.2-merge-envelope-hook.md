# 14.2 — mergeDraftEnvelope SDK hook

## Charter

Close Phase 13.0b deferral; generic envelope helpers use `WorkspaceWizardDraftEnvelope` only.

## SDK addition

```typescript
readonly mergeDraftEnvelope?: <TForm>(
  local: WorkspaceWizardDraftEnvelope<TForm>,
  server: WorkspaceWizardDraftEnvelope<TForm>
) => WorkspaceWizardDraftEnvelope<TForm>;
```

## Package move

Merge algorithm → `packages/workspaces/denali/src/draft/merge-envelope.ts`

## Acceptance

- `wizard-draft-envelope-hooks.ts` does not import `denali-wizard-draft-merge`
- Denali hook implements `mergeDraftEnvelope`
