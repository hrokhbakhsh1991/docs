# 14.0b — Label resolver manifest

## Charter

Separate field/step label resolution from composite surfaces; fix `wizard-field.tsx` hardcoded `denali` namespace.

## Host allowlist (GAP-17)

- `apps/web/src/wizard/workspace-wizard-host.tsx` — pass `translateWorkspaceMessage` to `WizardField`; use label registry for step labels

## Manifest block

```json
"wizardI18n": {
  "messageNamespace": "denali",
  "labelResolver": {
    "webModule": "@app-tour/workspace-denali/ui/field-label-resolver",
    "export": "createDenaliFieldLabelResolver"
  }
}
```

## Acceptance

- `wizard-template-field-labels.ts` has no `@app-tour/workspace-denali` import
- `wizard-field.tsx` has no `useTranslations("denali")`
