🗺️ شناسنامه فوق‌تفصیلی فازهای جراحی سیستم تمپلیت‌ها تا کمال مطلق🏁 فاز ۰: تثبیت هماهنگی لایه C و استقرار کارخانه هدلس اولیه (کامل شده ✅)تغییرات فیزیکی: تعریف ثابت DENALI_MODERN_SETTINGS_OVERLAY_STORAGE_PATHS (۵۱ مسیر مدرن) در پکیج دامین و حذف کدهای دوقلو و موازی از پوشه کلاینت (apps/web). پیاده‌سازی کلاس DenaliTemplateOrchestratorFactory.وضعیت پایداری: ۱۸ گارد ساختاری سبز، ۶۳ تست واحد پکیج دامین پاس شده.میزان ریسک: صفر (کدهای مرده با موفقیت هرس شدند).📡 فاز ۱: اتصال کارخانه به شبکه و اندپوینت NestJS (فاز جاری)هدف اصلی: خارج کردن کارخانهٔ هدلس از انزوای حافظه و فعال کردن آن در قالب یک سرویس متصل به پروتکل HTTP.فایل‌های هدف (تعییرات افزودنی):apps/api/src/modules/settings-locations/settings-locations.module.ts (تزریق یک‌طرفه DraftEngineModule)apps/api/src/modules/settings-locations/settings-tour-wizard-template.controller.ts (افزودن روت POST /instantiate)apps/api/src/modules/settings-locations/tour-wizard-template-settings.service.ts (پیاده‌سازی متد لود دیتای تمپلیت و فراخوانی کارخانه هدلس)خط لوله منطقی (Execution Wire):رایانه ادمین/کارمند درخواست را به روت جدید می‌فرستد $\rightarrow$ دیتای تمپلیت لود می‌شود $\rightarrow$ کارخانه هدلس در لایه حافظه دیتا را نرمالایز می‌کند $\rightarrow$ در صورت پاس شدن فلگ ?seedDraft=true، اسنپ‌شاتِ حاصل عینا در مخزن postgres-draft-snapshot.store.ts با ورژن ۰ سِید (Seed) می‌شود.تست پایداری و رگرسیون: روت‌های لایو GET و PATCH قبلی کاملاً دست‌نخورده باقی می‌مانند. کنترلر اصلی تورها (ToursController) اصلاً در این فاز ریفکتور نمی‌شود.🛡️ فاز ۲: سخت‌گیری حاکمیتی و مهار فسیل‌های داده‌ای در سرور (Governance Hardening)هدف اصلی: مسدود کردن دائمی کانال‌های تزریق فیلدهای شبح و قدیمی از طریق دیتابیس یا کلاینت‌های دستکاری‌شده، و اجباری کردن قوانین انتشار (Publish Gate) در بک‌آند.فایل‌های هدف:apps/api/src/modules/settings-locations/validate-workspace-wizard-template.tsapps/api/src/modules/settings-locations/tour-wizard-template-settings.service.tsتغییرات فیزیکی:خواندن فلگ publish: true در زمان PATCH تمپلیت در سمت سرور و اجرای متد ولیدیشن validatePublishGate در بک‌آند (در حال حاضر این گیت فقط در فرانت‌بند کلاینت اجرا می‌شود و سرور نسبت به آن کور است).فعال کردن یک فلگ اختیاری به نام registryStrictMode؛ اگر دیتای ورودی در دیتابیس حاوی کلیدهای قدیمی ریجکت‌شده (مانند startPointLocationText یا transportNotes) باشد، سرور خطای ۴۰۰ صادر کرده و اجازه ذخیره دیتای کثیف را نمی‌دهد.🔌 فاز ۳: پل زدن ویزارد اصلی کارمندان به کارخانه هدلس کلاینت (The Wizard Bridge)هدف اصلی: همگام‌سازی چرخه آغازین فرم ساخت تور با خروجی کارخانه هدلس و حل مشکل ناهماهنگی فیلدها در لود اولیه.فایل‌های هدف:apps/web/src/components/tours/wizard/TourCreateWizard.tsxapps/web/src/components/tours/wizard/WorkspaceTourWizard.tsxتغییرات فیزیکی:در فایل TourCreateWizard یک هوک جدید به نام useInstantiateWorkspaceTemplate متولد می‌شود که اندپوینت ساخته‌شده در فاز ۱ را صدا می‌زند.در فایل WorkspaceTourWizard (خطوط ۲۷۲-۲۸۷)، منطق جفت شدن فرم تغییر می‌کند؛ به جای صدا زدن مستقیم متد قدیمی خطی tryHydrateCanonicalTemplate، استیتِ اولیه از کارخانه هدلس فاز ۱ لود می‌شود.قانون ایمنی درفت‌ها (Draft Precedence): منطق ادغام درفت‌های لایو جاری کاملاً حفظ می‌شود: mergeDenaliFormDefaults(factoryForm, draftState.data.form)؛ یعنی اگر کارمندی وسط ساخت تور باشد، کارخانه دیتای تمپلیت او را خراب نکرده و درفت جاری او بالاترین اولویت (Win Precedence) را دارد.📐 فاز ۴: یکپارچه‌سازی لایه پروژکشن و نقشه فرانت‌بند (Unified Projection)هدف اصلی: از بین بردن خط لوله‌های موازی مپ کردنِ فرم کلاینت به دیتای شبکه و تضمین تطابق ۱:۱ با کدهای دامین.فایل‌های هدف:apps/web/src/features/tours/wizard/domain/buildDenaliCreateTourPayloadProjection.tsپکیج وبِ هوک‌های ثبت تور (useDenaliTourWizardCreate)تغییرات فیزیکی:این فایل در فرانت‌بند کلاً تبدیل به یک Re-export ساده از تابع دامنه‌ای buildDenaliCreateTourPayloadProjection در پکیج دامین می‌شود که پیش از این در فاز ۰ پورت شده بود.قبل از سوییچ نهایی، یک تست تفاضلی طلایی (Golden-File Diff Spec) نوشته می‌شود تا تضمین کند ساختار داده‌ای ارسالی کلاینت با چیزی که کارخانه هدلس تولید می‌کند، کوچک‌ترین انحراف ساختاری ندارد.🧬 فاز ۵: غنی‌سازی عمیق سرور و موتور کپی آنی (Deep Server Cloning)هدف اصلی: قطع وابستگی کامل بک‌آند به مرورگر کلاینت برای کپی کردن یا نمونه‌سازی از تورها، مدیریت درست شناسه‌های عکس‌ها و رفع کامل مشکل تورهای یتیم بدون تصویر.فایل‌های هدف:apps/api/src/modules/tours/tours.module.ts (وارد کردن ماژول درفت انجین)apps/api/src/modules/tours/services/tours-clone.service.tsapps/api/src/modules/tours/tours.service.tsتغییرات فیزیکی:فعال شدن روت نوین POST /api/v2/tours/clone/:sourceTourId در بک‌آند.متد ToursCloneService با کارخانه هدلس ترکیب می‌شود. این متد ساختار تو در توی trip_details را می‌خواند، شناسه‌های عکس‌ها را در سطح آبجکت استوریج کپی و Remint (تولید UUID جدید برای جلوگیری از تعارض مالکیت فرعی) می‌کند، زمان‌ها را با مکانیزم splitIsoDateTime خرد کرده و بدون نیاز به سوار شدن چرخه‌های رندر فرانت‌بند، نمونهٔ نوین و بالغ تور را مستقیماً در دیتابیس ثبت می‌کند.