/**
 * Storybook / visual-test helpers — not published (excluded from `tsconfig.build.json`).
 * Import via relative paths within this package in tests/Storybook only.
 */
export {
  validatedWorkspacePresetStyle,
  validatedWorkspaceThemeStyle,
} from "./validated-workspace-theme-style";
