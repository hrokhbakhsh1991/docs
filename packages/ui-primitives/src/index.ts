/**
 * @deprecated Barrel entry is not published. Import primitives via subpaths only:
 * `@app-tour/ui-primitives/button`, `/input`, `/field-shell`, `/alert`, `/badge`.
 *
 * This file is excluded from `tsconfig.build.json` and omitted from `package.json` `exports`.
 */
export {};
