/** CW9-03 — cert-club registration transport initializer stub. */
export function registerCertClubCatalogRegistrationTransportInitializer(): void {
  // Synthetic cert fixture — transport intake defaults only; no host registration.
}
