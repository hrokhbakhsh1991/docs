/**
 * Booking-ws2 workspace type constant — architecture fixture (Phase B1.3).
 * Manifest: registryOnly — capability + dependency bindings only (no product plugin/nav).
 */
export const BOOKING_WS2_WORKSPACE_TYPE = "booking-ws2";
