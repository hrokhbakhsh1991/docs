/**
 * Contract placeholder — manifest binding only; no wallet ledger runtime (Phase 1).
 */
export class DenaliWalletLedgerPolicyAdapter {
  readonly kind = "denali-wallet-ledger-policy" as const;
}
