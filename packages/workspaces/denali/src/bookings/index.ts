/**
 * Operator bookings ops presentation (command center).
 * Business lifecycle / capacity policy lives in `../booking` — do not duplicate rules here.
 */
export {
  DEFAULT_BOOKING_OPS_MANIFEST,
  denaliRegistrationOpsManifest,
  getDenaliRegistrationOpsManifest,
  resolveBookingOpsManifestFromTheme,
} from "./ops-manifest";
