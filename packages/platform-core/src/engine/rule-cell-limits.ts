/** Upper bound on ruleSet.cells indexed per workspace plugin. */
export const MAX_RULE_CELL_INDEX_SIZE = 256;

/** Upper bound on fieldRegistry.fields per workspace plugin. */
export const MAX_ALLOWED_REGISTRY_FIELDS = 1000;
