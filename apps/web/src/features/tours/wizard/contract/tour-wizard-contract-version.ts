/**
 * Bump when the wizard root object shape or cross-field rules change in a breaking way
 * (keep in sync with persisted `_wizardMeta.wizardContractVersion` defaults in draft parsing).
 */
export const TOUR_WIZARD_CONTRACT_VERSION = 1 as const;
