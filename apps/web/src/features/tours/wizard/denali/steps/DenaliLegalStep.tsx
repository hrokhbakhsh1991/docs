"use client";

/** Wizard step `denali_legal` — policies and cancellation terms (UI migration in progress). */
export function DenaliLegalStep() {
  return (
    <div
      data-testid="denali-step-legal"
      style={{ display: "grid", gap: "0.85rem" }}
    >
      <p style={{ margin: 0, fontSize: "0.85rem", color: "#64748b" }}>
        قوانین و شرایط — این بخش به‌زودی تکمیل می‌شود.
      </p>
    </div>
  );
}
