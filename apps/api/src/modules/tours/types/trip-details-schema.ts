/**
 * Single source for persisted `tour_details.trip_details` JSON schema version.
 * Bump when breaking shape changes; migrations may backfill older documents.
 */
export const CURRENT_TRIP_DETAILS_SCHEMA_VERSION = 1 as const;
