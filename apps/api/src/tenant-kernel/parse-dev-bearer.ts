/** @deprecated Import from `./parse-bearer` — re-export for existing tests. */
export {
  DEV_BEARER_PREFIX,
  encodeDevBearerToken,
  isDevBearerAuthorization,
  tryParseDevBearerToken,
} from "./parse-bearer";
