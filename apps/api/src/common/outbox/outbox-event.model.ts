import type { OutboxEventEntity } from "./entities/outbox-event.entity";

/** Type alias for documentation / ports — persisted row is {@link OutboxEventEntity}. */
export type OutboxEvent = OutboxEventEntity;
