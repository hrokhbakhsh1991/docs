/**
 * Read tour canonical `allowMembershipDiscount` gate (DEC-CQ-011).
 * Re-export from finance-core domain — host adapter stays workspace-agnostic.
 */
export { readTourAllowMembershipDiscount } from "@app-tour/finance-core/domain";
